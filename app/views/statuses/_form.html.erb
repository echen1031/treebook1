<%= simple_form_for(@status, html: {class: "form-horizontal"}) do |f| %>

  <fieldset>
    <legend>Status Update</legend>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<<<<<<< HEAD
  <%= f.input :user_id, collection: User.all, label_method: :full_name %>
  <%= f.input :content %>
=======
 <%= f.association :user, collection: User.all, prompt: "Choose a User", label_method: :profile_name, value_method: :id %>
 <%= f.input :content %>

>>>>>>> 4473e6b1275465f4942c724d3c817a9778838bbc
 <div class="form-actions">
   <%= f.button :submit %>
  </div>
  </fieldset>
<% end %>
